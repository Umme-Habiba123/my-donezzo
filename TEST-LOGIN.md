# 🧪 লগইন টেস্ট করার নির্দেশনা

## সমস্যা যা ছিল:
1. ❌ লগইন করার পরে ড্যাশবোর্ড দেখা যাচ্ছিল না
2. ❌ লগআউট ক্লিক করলে `#` আসছিল URL-এ

## যা ঠিক করা হয়েছে:
1. ✅ Sidebar-এ সব `<a href="#">` বদলে `<button>` করা হয়েছে
2. ✅ লগআউট এখন সঠিকভাবে কাজ করবে এবং লগইন পেজে নিয়ে যাবে
3. ✅ ডিবাগিং লগ যোগ করা হয়েছে (browser console-এ দেখতে পারবেন)
4. ✅ User data সঠিকভাবে localStorage-এ সেভ হবে

## টেস্ট করার ধাপ:

### ১. সার্ভার চালু করুন
```
Double-click: START-SERVER.bat
```

### ২. ব্রাউজার খুলুন
```
http://localhost:5173
```

### ৩. Browser Console খুলুন (গুরুত্বপূর্ণ!)
```
Press F12 or Right-click → Inspect → Console tab
```

### ৪. লগইন করুন
- Email এবং Password দিন
- "Sign In" বাটনে ক্লিক করুন
- Console-এ এই মেসেজগুলো দেখবেন:
  ```
  🔐 Login attempt with: {email, password}
  📥 API Response: {...}
  ✅ Login successful!
  💾 Stored in localStorage: {...}
  🚀 Navigating to /dashboard
  🔒 ProtectedRoute check: {hasToken: true}
  ✅ Token found, rendering protected content
  📊 Dashboard mounted
  💾 Loading from localStorage: {...}
  ```

### ৫. ড্যাশবোর্ড চেক করুন
- ড্যাশবোর্ড দেখা যাচ্ছে কিনা
- Header-এ আপনার নাম/email দেখা যাচ্ছে কিনা
- URL হবে: `http://localhost:5173/dashboard`

### ৬. লগআউট টেস্ট করুন
- Sidebar-এ "Logout" বাটনে ক্লিক করুন
- Console-এ দেখবেন: `🚪 Logging out...`
- লগইন পেজে ফিরে যাবে
- URL হবে: `http://localhost:5173/` (কোন `#` থাকবে না!)

## যদি সমস্যা হয়:

### লগইন করার পরেও ড্যাশবোর্ড না দেখায়:
1. Console খুলুন (F12)
2. দেখুন কোন error আছে কিনা
3. Console-এ লাল রঙের মেসেজ থাকলে সেটা কপি করে দেখান

### API Error দেখালে:
```
Check:
1. Internet connection আছে কিনা
2. API URL সঠিক: https://task-api-eight-flax.vercel.app/api/login
3. Email/Password সঠিক কিনা
```

### localStorage চেক করতে:
1. F12 → Application tab (Chrome) বা Storage tab (Firefox)
2. Local Storage → http://localhost:5173
3. দেখবেন:
   - `token`: "your-jwt-token"
   - `user`: {"email": "...", "name": "..."}

## ডিবাগিং টিপস:

### Console Messages বুঝুন:
- 🔐 = Login attempt
- 📥 = API response received
- ✅ = Success
- ❌ = Error
- 💾 = Data saved
- 🚀 = Navigation
- 🔒 = Route protection check
- 📊 = Dashboard loaded
- 🚪 = Logout

### Manual Test:
Console-এ এই কমান্ড চালান:
```javascript
// Check if token exists
console.log("Token:", localStorage.getItem("token"));

// Check if user exists
console.log("User:", localStorage.getItem("user"));

// Clear everything (if needed)
localStorage.clear();
```

## সফল লগইন এর লক্ষণ:

✅ Console-এ সবুজ চেকমার্ক (✅) দেখা যাবে
✅ URL বদলে `/dashboard` হবে
✅ ড্যাশবোর্ড UI দেখা যাবে
✅ Header-এ user info দেখা যাবে
✅ Sidebar-এ সব মেনু দেখা যাবে

## সফল লগআউট এর লক্ষণ:

✅ Console-এ "🚪 Logging out..." দেখা যাবে
✅ URL বদলে `/` হবে (কোন `#` নেই!)
✅ লগইন পেজ দেখা যাবে
✅ localStorage খালি হয়ে যাবে

---

এখন টেস্ট করুন এবং Console-এ কি দেখছেন সেটা জানান!
